# TrafficModel

### Quick Start

```bash
cd model
python train.py -d PEMS08 -m train
```

### Test

```bash
cd model
python train.py -d PEMS08 -m test
```

### Data

Place dataset files under `data/<DATASET_NAME>/` (for example `data/PEMS08/data.npz`).
